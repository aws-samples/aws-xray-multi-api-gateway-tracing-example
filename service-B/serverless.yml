service: service-B

frameworkVersion: '2'

provider:
  name: aws
  runtime: nodejs12.x
  lambdaHashingVersion: '20201221'
  tracing:
    apiGateway: true

functions:
  hello:
    handler: handler.hello
    events:
      - http:
          path: /
          method: get
